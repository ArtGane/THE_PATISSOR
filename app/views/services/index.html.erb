<%= Service.all %>

<ul>
    <% @services.each do |service| %>
    <li><%= service.name %></li>
    <li><%= service.description %></li>
    <li><%= service.price_per_hour %></li>
    <li><%= service.localisation %></li>
    <%= link_to "<button>Choisis moi</button>".html_safe, new_service_rent_path(service) %>
    <% end %>
      <%= link_to "Home", root_path %>
</ul>